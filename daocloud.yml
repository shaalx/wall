image: daocloud.io/library/golang:1.6.0

env:
  - GOPATH = /app/gopath/
  - PATH = $PATH:$GOPATH/bin

install:
  - echo $PATH
  - echo $GOPATH
  - go get ./...

script:
  - go version
  - pwd
  - ls
  - go run script.go
