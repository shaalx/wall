image: daocloud.io/library/golang:1.6.0

env:
  - GOPATH = /app/gopath/
  - PATH = $PATH:$GOPATH/bin

install:
  - echo $PATH
  - echo $GOPATH
  - go get -u github.com/shaalx/wall

script:
  - go version
  - pwd
  - ls
  - go test -v
