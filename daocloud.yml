image: daocloud.io/library/golang:1.6.0

env:
  - GOPATH = /app/gopath/
  - PATH = $PATH:$GOPATH/bin
  - WORKDIR /app/gopath/src/github.com/shaalx/wall

install:
  - echo $PATH
  - echo $GOPATH
  - echo $WORKDIR

script:
  - go env
  - go version
  - pwd
